# hackthe6ix

Run the Python app with:

```bash
python main.py
python main.py Karen
```

The script accepts an optional name argument and defaults to "world" if none is provided.

Run the tests with:

```bash
python -m unittest discover -s tests
```